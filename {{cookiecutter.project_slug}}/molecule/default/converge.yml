---
- name: Converge
  hosts: all
  gather_facts: true

  pre_tasks:
    - name: Update cache (Debian)
      ansible.builtin.apt:
        update_cache: true
        cache_valid_time: 600
      when: ansible_facts.os_family == "Debian"

  roles:
    - {{ cookiecutter.github_username }}.{{ cookiecutter.role_name }}
